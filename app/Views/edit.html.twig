<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>イベント編集</title>
</head>

<body>
    <h1>イベント編集</h1>

    <form action="/OshiCal/public/index.php?route=event/update" method="post">
        <input type="hidden" name="id" value="{{ event.id }}">

        <p>
            タイトル：<br>
            <input type="text" name="title" value="{{ event.title }}">
        </p>

        <p>
            日付：<br>
            <input type="text" name="date" value="{{ event.date }}">
        </p>

        <p>
            メモ：<br>
            <textarea name="memo" rows="5">{{ event.memo }}</textarea>
        </p>

        <p>
            <button type="submit">更新する</button>
        </p>
    </form>
</body>
</html>
